-- Date Updated -- 8:00 PM -- 9/3/2026 -- DD/MM/YYYY

if not game:IsLoaded() then game.Loaded:Wait() end

local players = game:GetService("Players")
local tws = game:GetService("TweenService")
local rs = game:GetService("RunService")
local rp = game:GetService("ReplicatedStorage")

local plr = players.LocalPlayer
local char
local hrp
local hum

if plr.PlayerGui:FindFirstChild("NickAAC") then return end

local lobby = false

function checking(t, f, fallback)
	if type(f) == t then return f end
	return fallback
end

function call_module(path)
    local suc, mod = pcall(require, path)
    if suc then return mod end
end

local queueteleport = checking("function", queue_on_teleport or (syn and syn.queue_on_teleport) or (fluxus and fluxus.queue_on_teleport))

local modifiers_module = call_module(rp.Dictionaries.GameModifiers)
local classes_module = call_module(rp.Dictionaries.Classes)
local enemies_module = call_module(rp.Dictionaries.Enemies)
local abilities_module = call_module(rp.Dictionaries.Abilities)

local ProximityPrompts_Screen = plr.PlayerGui:FindFirstChild("ProximityPrompts")

if not ProximityPrompts_Screen then
	ProximityPrompts_Screen = Instance.new("ScreenGui")
	ProximityPrompts_Screen.Name = "ProximityPrompts"
	ProximityPrompts_Screen.ResetOnSpawn = false
	ProximityPrompts_Screen.IgnoreGuiInset = true
	ProximityPrompts_Screen.Parent = plr.PlayerGui
end

local place_ids = {
	80734098185936 , --Main Lobby
	111943251737481, --In Game
}

local workspace_debug

if table.find(place_ids, game.PlaceId) then
	workspace_debug = workspace
	if game.PlaceId == 80734098185936 then
		lobby = true
	end
else
	workspace_debug = workspace:FindFirstChild("Simulate"):FindFirstChild("Game")
end

-----Settings-----
local Minimized = false
local DESTROYED = false
local current_catagory = nil
local run_add_decent = true
local run_remove_decent = true
local auto_play = false
local auto_afk = true
local current_screen = nil
local frame_store = {}
local modifiers_table = {}
local party_info = {
			PartySize = 1;
			FriendsOnly = true;
			StashCraft = true;
			GameSpeed = 2;
		}
local skills = {
	["Strike"] = 0,
	["Guard"] = 0,
}
local plr_stats = {
	["Class"] = nil,
	["Energy"] = nil,
	["EXP"] = nil,
	["Gold"] = nil,
	["HP"] = nil,
	["isAlive"] = nil,
	["isLoaded"] = nil,
	["Level"] = nil,
	["MaxEnergy"] = nil,
	["MaxHP"] = nil,
	["StatPoints"] = nil,
	["Turn"] = nil,
}
local background_image_id = "rbxassetid://86093393173023"
local truecolor = Color3.fromRGB(170, 170, 0)
local falsecolor = Color3.fromRGB(163, 162, 165)
-----Settings-----

local runners = {
	runner_sceneevent = nil,
	runner_changeuievent = nil,
	runner_combatturnevent = nil,
	runner_ontp = nil,
}

-----SetUp GUI-----

local screen = Instance.new("ScreenGui")
screen.Name = "NickAAC"
screen.DisplayOrder = 10000000
screen.ResetOnSpawn = false
screen.Parent = plr.PlayerGui

local main_frame : Frame
local catagories_frame : Frame
local farming_frame : Frame

local show_button : TextButton
local close_button : TextButton
local minimize_button : TextButton

local quick_play : TextButton
local auto_play_button : TextButton
local auto_afk_button : TextButton

--PreLoad Local Universals Function:
local function UiCorner(Parent, Ui)
	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = Ui
	Corner.Parent = Parent
	return Corner
end

local function UiStroke(Parent)
	local Stroke = Instance.new("UIStroke")
	Stroke.Color = Color3.fromRGB(255, 255, 255)
	Stroke.Thickness = 1
	Stroke.LineJoinMode = Enum.LineJoinMode.Miter
	Stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
	Stroke.Parent = Parent
	return Stroke
end

local function create_button_catagory(parent, name: string, frame)
	local button = Instance.new("TextButton")
	button.Text = name
	button.TextColor3 = Color3.fromRGB(255, 255, 255)
	button.TextScaled = true
	button.TextStrokeTransparency = 0
	button.TextColor3 = Color3.fromRGB(255, 255, 255)
	button.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
	button.Parent = parent
	UiStroke(button)
	
	button.Activated:Connect(function()
		if current_catagory and current_catagory ~= frame then
			current_catagory.Visible = false
			current_catagory = frame
			current_catagory.Visible = true
		else
			current_catagory = frame
			current_catagory.Visible = true
		end
	end)
	
	return button
end

local function create_frame(parent, title: string, description: string, value: any, extra_value: any)
	local frame = Instance.new("Frame")
	frame.BackgroundColor3 = Color3.fromRGB(75, 75, 75)
	frame.Parent = parent
	UiStroke(frame)

	local title_label = Instance.new("TextLabel")
	title_label.Text = title
	title_label.TextColor3 = Color3.fromRGB(255, 255, 255)
	title_label.TextStrokeTransparency = 0
	title_label.TextXAlignment = Enum.TextXAlignment.Left
	title_label.BackgroundTransparency = 1
	title_label.RichText = true
	title_label.TextScaled = true
	title_label.Position = UDim2.new(0,0,0,0)
	title_label.Size = UDim2.new(.75,0,.6,0)
	title_label.Parent = frame

	local description_label

	if description then
		description_label = Instance.new("TextLabel")
		description_label.Text = description
		description_label.TextColor3 = Color3.fromRGB(200, 200, 200)
		description_label.TextXAlignment = Enum.TextXAlignment.Left
		description_label.BackgroundTransparency = 1
		description_label.RichText = true
		description_label.TextScaled = true
		description_label.Position = UDim2.new(0,0,.6,0)
		description_label.Size = UDim2.new(.75,0,.4,0)
		description_label.Parent = frame
	end

	local function make_value(value)
		local button
		if value == nil then return end

		if typeof(value) == "boolean" then
			button = Instance.new("TextButton")
			button.Text = "  "
			button.BackgroundColor3 = if value then truecolor else falsecolor
		elseif typeof(value) == "number" then
			button = Instance.new("TextBox")
			button.Text = tostring(value)
		end

		if value == "Click" then
			button = Instance.new("TextButton")
			button.Text = ">"
			button.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
			button.TextScaled = true
		elseif value == "Select" then
			button = Instance.new("TextButton")
			button.Text = "None"
			button.TextScaled = true
			button.TextWrapped = true
		end

		return button
	end

	local e1 = make_value(value)
	local e2 = make_value(extra_value)

	if e2 then
		title_label.Size = UDim2.new(.5,0,.6,0)
		if description_label then
			description_label.Size = UDim2.new(.5,0,.4,0)
		end

		e1.Position = UDim2.new(.75,0,0,0)
		e1.Size = UDim2.new(.25,0,1,0)
		e1.Parent = frame

		e2.Position = UDim2.new(.5,0,0,0)
		e2.Size = UDim2.new(.25,0,1,0)
		e2.Parent = frame
		return e1, e2
	else
		title_label.Size = UDim2.new(.75,0,.6,0)

		if description_label then
			description_label.Size = UDim2.new(.75,0,.4,0)
		end

		e1.Position = UDim2.new(.75,0,0,0)
		e1.Size = UDim2.new(.25,0,1,0)
		e1.Parent = frame
		return e1
	end
end

local function character_available()
	local available = false
	char = plr.Character
	if not char then return available end
	hrp = char:FindFirstChild("HumanoidRootPart") :: Part
	hum = char:FindFirstChildOfClass("Humanoid") :: Humanoid

	if hrp and hum then
		available = true
	end

	return available

end

--Preload InGame Function:

-- Start the game when player join
local aac_start = function()
	rp.Remotes.VotingEvent:FireServer("Start Game")
end

-- Ready when it rest menu
local aac_ready = function()
	if current_screen == "Rest" then
		rp.Remotes.VotingEvent:FireServer("Rest")
	end
end

-- Choose option path from 1 to 3 tbl_upvw[1] to tbl_upvw[3] or can be title name like "Busted Caravan"
local aac_encounter_choice = function(option)
	if current_screen == "Encounter" then
		rp.Remotes.VotingEvent:FireServer("Encounter", option)
	end
end

-- Choose the choice when the menu option Appear for example "Attack while their guard is down"
local aac_choice = function(choice_name)
	if current_screen == "Choice" then
		rp.Remotes.VotingEvent:FireServer("Scene", choice_name)
		rp.Remotes.VotingEvent:FireServer("SceneClient", choice_name)
	end
end

-- in Combat goal like "Ability" "Focus"
-- enemy are the character model of Enemy
-- Skill Name like "Flame Bolt"
local aac_combat = function(goal, enemy, skill_name)
	rp.Remotes.TurnDecision:FireServer(goal, enemy, skill_name)
end

-- Replay the Game if player die
local aac_replay = function()
	rp.Remotes.ReplayEvent:FireServer()
end

local AI_Auto_Play = function()
	character_available()
	if auto_play then
		while task.wait() do
			if not auto_play then break end
			if not plr_stats.isLoaded then continue end

			-- in Combat
			-- rp.Remotes.TurnDecision:FireServer("Focus")
			-- rp.Remotes.TurnDecision:FireServer("Ability", workspace.Enemies.Pebble, "Flame Bolt")
			
		end
	end
end

local Afk_Auto_Play = function()
	character_available()
	if auto_afk then
		while task.wait() do
			if not auto_afk then break end
			if not plr_stats.isLoaded then continue end
			
			if current_screen == "Title" then
				aac_start()
			end
			
			if current_screen == "Rest" then
				aac_ready()
				current_screen = nil
			end
			
			if current_screen == "Encounter" then
				local child = {
					plr.PlayerGui.EncounterGUI.EncounterFrame.Background.Option1,
					plr.PlayerGui.EncounterGUI.EncounterFrame.Background.Option2,
					plr.PlayerGui.EncounterGUI.EncounterFrame.Background.Option3,
				}
				local chosen = child[math.random(1, #child)]
				aac_encounter_choice(chosen.Title.Text)
				current_screen = nil
			end
			
			if current_screen == "Choice" then
				task.wait(1)
				local child = {}
				for _,v in pairs(plr.PlayerGui.SceneGUI.Background.Options:GetChildren()) do
					if v:IsA("UIListLayout") then continue end
					if v.Name == "redpill" then continue end --// Avoid Getting out of the matrix and stop the process
					table.insert(child, v)
				end
				if not child[1] then continue end
				local chosen = child[math.random(1, #child)]
				aac_choice(chosen.Name)
				task.wait(.25)
			end
			
			if current_screen == "Combat" then
				if plr:GetAttribute("Turn") then
					local enemy = workspace.Enemies:GetChildren()[1]
					local allies = {}
					local t_skill = {}
					for _,v in pairs(plr.PlayerGui.PlayerInfo.AbilitiesFrame.AbilitiesScrollingFrame:GetChildren()) do
						if v:IsA("UIListLayout") then continue end
						if not skills[v.Name] then
							skills[v.Name] = 0
						end
					end
					
					for i,v in pairs(skills) do
						if v ~= 0 then skills[i] -= 1 continue end
						if i == "Guard" then continue end
						if abilities_module[i].TargetType == "SingleAlly" then continue end
						if plr:GetAttribute("Energy") < abilities_module[i].Cost then continue end
						table.insert(t_skill, i)
					end
					local chosen = t_skill[math.random(1, #t_skill)]
					skills[chosen] = abilities_module[chosen].Cooldown

					if abilities_module[i].TargetType == "Self" then
						aac_combat("Ability", plr.Character, chosen)
					else
						aac_combat("Ability", enemy, chosen)
					end
					task.wait(1)
				end
			end
			
			if current_screen == "Over" then
				aac_replay()
				current_screen = nil
				break
			end
			
		end
	end
end

-----// SetUp GUI:
do
	main_frame = Instance.new("Frame")
	main_frame.Draggable = true
	main_frame.Name = "Main"
	main_frame.Active = true
	main_frame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
	main_frame.AnchorPoint = Vector2.new(1, 0)
	main_frame.Position = UDim2.new(1, 0, 0.15, 0)
	main_frame.Size = UDim2.new(0.25, 0, 0.4, 0)
	main_frame.Parent = screen
	UiStroke(main_frame)
	
	back_ground_frame = Instance.new("ImageLabel")
	back_ground_frame.BackgroundTransparency = 1
	back_ground_frame.ImageTransparency = .75
	back_ground_frame.Image = background_image_id
	back_ground_frame.Position = UDim2.new(0, 0, 0.1, 0)
	back_ground_frame.Size = UDim2.new(1,0,0.9,0)
	back_ground_frame.Parent = main_frame
	
	do -----Main Frame

		close_button = Instance.new("TextButton")
		close_button.Text = "X"
		close_button.AnchorPoint = Vector2.new(1,0)
		close_button.BackgroundTransparency = 1
		close_button.TextScaled = true
		close_button.TextColor3 = Color3.fromRGB(255, 170, 0)
		close_button.Position = UDim2.new(1,0,0,0)
		close_button.Size = UDim2.new(0.05, 0,0.1, 0)
		close_button.Parent = main_frame

		minimize_button = Instance.new("TextButton")
		minimize_button.Text = "-"
		minimize_button.AnchorPoint = Vector2.new(1,0)
		minimize_button.BackgroundTransparency = 1
		minimize_button.TextScaled = true
		minimize_button.TextColor3 = Color3.fromRGB(255, 170, 0)
		minimize_button.Position = UDim2.new(.95,0,0,0)
		minimize_button.Size = UDim2.new(0.1,0,0.1,0)
		minimize_button.Parent = main_frame

		farming_frame = Instance.new("ScrollingFrame")
		farming_frame.Active = true
		farming_frame.Visible = true
		farming_frame.BackgroundTransparency = .25
		farming_frame.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
		farming_frame.Position = UDim2.new(0.25, 0, 0.1, 0)
		farming_frame.Size = UDim2.new(.75,0,0.9,0)
		farming_frame.CanvasSize = UDim2.new(0,0,0,0)
		farming_frame.AutomaticCanvasSize = Enum.AutomaticSize.Y
		farming_frame.ScrollBarThickness = 0
		farming_frame.Parent = main_frame
		UiStroke(farming_frame)

		do
			local grid = Instance.new("UIGridLayout")
			grid.CellPadding = UDim2.new(0,0,0,1)
			grid.CellSize = UDim2.new(1,0,.15,0)
			grid.FillDirection = "Horizontal"
			grid.HorizontalAlignment = Enum.HorizontalAlignment.Center
			grid.VerticalAlignment = Enum.VerticalAlignment.Top
			grid.Parent = farming_frame
			
			if lobby then
				quick_play = create_frame(farming_frame, "Quick play", "Quick play with all the hardest modifirers", "Click")
			end

			auto_play_button = create_frame(farming_frame, "Auto Play (Not Work Rn)", "Auto Play (Not Work Rn)", auto_play)
			
			auto_afk_button = create_frame(farming_frame, "Auto AFK", "Auto AFK", auto_afk)

		end
		
		settings_frame = Instance.new("ScrollingFrame")
		settings_frame.Active = true
		settings_frame.Visible = false
		settings_frame.BackgroundTransparency = .25
		settings_frame.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
		settings_frame.Position = UDim2.new(0.25, 0, 0.1, 0)
		settings_frame.Size = UDim2.new(.75,0,0.9,0)
		settings_frame.CanvasSize = UDim2.new(0,0,0,0)
		settings_frame.AutomaticCanvasSize = Enum.AutomaticSize.Y
		settings_frame.ScrollBarThickness = 0
		settings_frame.Parent = main_frame
		UiStroke(settings_frame)

		do
			local grid = Instance.new("UIGridLayout")
			grid.CellPadding = UDim2.new(0,0,0,1)
			grid.CellSize = UDim2.new(1,0,.15,0)
			grid.FillDirection = "Horizontal"
			grid.HorizontalAlignment = Enum.HorizontalAlignment.Center
			grid.VerticalAlignment = Enum.VerticalAlignment.Top
			grid.Parent = settings_frame


		end
		
		catagories_frame = Instance.new("ScrollingFrame")
		catagories_frame.Active = true
		catagories_frame.BackgroundTransparency = .25
		catagories_frame.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
		catagories_frame.Position = UDim2.new(0, 0, 0.1, 0)
		catagories_frame.Size = UDim2.new(.25,0,0.9,0)
		catagories_frame.CanvasSize = UDim2.new(0,0,0,0)
		catagories_frame.AutomaticCanvasSize = Enum.AutomaticSize.Y
		catagories_frame.ScrollBarThickness = 0
		catagories_frame.Parent = main_frame
		UiStroke(catagories_frame)

		do
			local grid = Instance.new("UIGridLayout")
			grid.CellPadding = UDim2.new(0,0,0,1)
			grid.CellSize = UDim2.new(1,0,.15,0)
			grid.FillDirection = "Horizontal"
			grid.HorizontalAlignment = Enum.HorizontalAlignment.Center
			grid.VerticalAlignment = Enum.VerticalAlignment.Top
			grid.Parent = catagories_frame
			
			current_catagory = farming_frame

			create_button_catagory(catagories_frame, "Farming", farming_frame)
			
			create_button_catagory(catagories_frame, "Settings", settings_frame)

		end
		
	end

	show_button = Instance.new("TextButton")
	show_button.Active = true
	show_button.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
	show_button.TextColor3 = Color3.fromRGB(255, 170, 0)
	show_button.Size = UDim2.new(0.05, 0, 0.085, 0)
	show_button.Text = "Show"
	show_button.Visible = false
	show_button.Parent = screen
	UiCorner(show_button, UDim.new(0.1,0))

end
-----// Startup:
if not lobby then
	for i,v in pairs(plr_stats) do
		plr_stats[i] = plr:GetAttribute(i)

		plr:GetAttributeChangedSignal(i):Connect(function()
			plr_stats[i] = plr:GetAttribute(i)
		end)
	end

	if plr.PlayerGui:FindFirstChild("TitleScreen") then
		current_screen = "Title"
	end

	if auto_afk then
		task.spawn(Afk_Auto_Play)
	end

	task.delay(2,function()
		if not plr_stats.isLoaded then
			plr_stats.isLoaded = true
		end
	end)
end

--Function:
close_button.MouseButton1Down:Connect(function()
	for i,v in pairs(runners) do
		if not runners[i] then continue end
		runners[i]:Disconnect()
	end
	auto_play = false
	auto_afk = false
	DESTROYED = true

	screen:Destroy()
end)

minimize_button.MouseButton1Click:Connect(function()
	Minimized = true
	main_frame.Visible = false
	show_button.Visible = true

	for _,v in pairs(screen:GetChildren()) do
		if v == main_frame then continue end
		if v == show_button then continue end
		if frame_store[v] then continue end
		frame_store[v] = v.Visible
		v.Visible = false
	end
end)

show_button.Activated:Connect(function()
	Minimized = false
	main_frame.Visible = true
	show_button.Visible = false

	for i,v in pairs(frame_store) do
		i.Visible = v
	end

	frame_store = {}
end)

if quick_play then
	quick_play.Activated:Connect(function()
		for i, v in pairs(modifiers_module) do
			if i == "Level 1" then continue end
			if v.CealtGain < 0 then continue end
			if v.PrestigeGain < 0 then continue end
			modifiers_table[i] = true
		end
		--// Create the party
		rp.Remotes.CreateParty:FireServer(party_info)
		task.wait(1)
		--// Modify party mode
		rp.Remotes.PartyModifiers:FireServer(modifiers_table)
		task.wait(1)
		--// Start the game
		rp.Remotes.PartyStart:FireServer()
	end)
end

if auto_play_button then
	auto_play_button.Activated:Connect(function()
		auto_play = not auto_play
		auto_play_button.BackgroundColor3 = auto_play and truecolor or falsecolor
	
		AI_Auto_Play()
	end)
end

if auto_afk_button then
	auto_afk_button.Activated:Connect(function()
		auto_afk = not auto_afk
		auto_afk_button.BackgroundColor3 = auto_afk and truecolor or falsecolor

		Afk_Auto_Play()
	end)
end

--///// Settings

-----Passive:
if not lobby then
	runners.runner_sceneevent = rp.Remotes.SceneEvent.OnClientEvent:Connect(function(goal)
		if goal == "OpenEncounterUI" then
			current_screen = "Encounter"
		elseif goal == "OpenSceneUI" then
			current_screen = "Choice"
		elseif goal == "OpenRestUI" then
			current_screen = "Rest"
		elseif goal == "CloseRestUI" then
			--current_screen = "Encounter"
		elseif goal == "CloseEncounterUI" then
			--current_screen = "Choice"
		elseif goal == "CloseSceneUI" then
			current_screen = "Combat" --Maybe Combat? or nil?
		end
	end)

	runners.runner_changeuievent = rp.Remotes.ChangeUI.OnClientEvent:Connect(function(goal)
		if goal == "CloseTitle" then
			current_screen = "Combat"
		elseif goal == "GameOver" then
			current_screen = "Over"
		end
	end)

	runners.runner_combatturnevent = rp.Remotes.FireTurn.OnClientEvent:Connect(function()
		if current_screen ~= "Combat" then
			current_screen = "Combat"
		end
	end)
end

runners.runner_ontp = plr.OnTeleport:Once(function()
	if queueteleport then
		queueteleport("loadstring(game:HttpGet('https://raw.githubusercontent.com/Soulless268/AAC/refs/heads/main/Source'))()")
	end
end)